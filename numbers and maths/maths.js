console.log(Math);

// abs -> absolute function :  -ve to +ve
// round -> round off to nearest integer
// ceil -> rounds off to next integer
// floor -> rounds off to prev integer
// min(a,b), max(a,b)

// RANDOM
console.log(Math.floor(Math.random()*10) + 1);

const min = 1
const max = 6
console.log(Math.floor((Math.random() * (max - min + 1)) + min));;